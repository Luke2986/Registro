# Lavoro rimandato

Voci emerse durante le revisioni e rimandate di proposito, con il motivo. Si cancellano solo quando sono chiuse.

## Deferred from: code review of 1-1-creare-un-cliente-col-solo-nome (2 agosto 2026)

- **`.btn` è alto 40px, sotto il bersaglio da 44px** — `src/app/globals.css:299-311`. Governa tutti e quattro i pulsanti nuovi di questa story (`Salva`, `Annulla`, `Nuovo cliente`, `Crea il primo cliente`). Rimandato: la classe è preesistente e fuori dal perimetro, e cambiarla tocca ogni schermata. Appartiene alla Story 5.2, passata di accessibilità verificata. **Annotazione dalla Story 3.2, il 9 agosto 2026, corretta dalla sua revisione**: AC6 della 3.2 chiede i 44px sulla schermata di compilazione ed è soddisfatta **senza toccare `.btn`** — ma solo in due dei quattro stati, e la prima stesura di questa annotazione diceva «quella schermata non ha nessun pulsante… è l'unica schermata del progetto di cui questo sia vero», che è falso. Nello stato **pieno** e in quello **vuoto** non c'è davvero nessun pulsante: nessuna azione primaria (non si salva ancora), nessuna freccia, nessun `Modifica`. Nei due stati d'**errore** invece `ErrorState` rende `Riprova` come `btn btn--secondary` (`components/error-state.tsx:11`), quindi il `.btn` a 40px c'è, esattamente come su ogni altra schermata. Chi chiuderà la voce non deve leggere la soddisfazione di AC6 come una prova che i 40px vadano bene, e nemmeno come una schermata da saltare quando ricontrollerà i bersagli.
- **L'hover illumina tutta la riga ma solo il nome è cliccabile** — `src/app/(app)/clienti/page.tsx:77-79` con `src/app/globals.css:250-252`. L'affordance promette una riga cliccabile, il comportamento dà una cella. Rimandato: la scelta fra estendere il collegamento alla riga o restringere l'hover dipende da come le Story 1.2 e 1.7 trattano la navigazione di riga.
- ~~**Nel ramo d'errore dell'elenco non resta nessun modo di creare un cliente**~~ — `src/app/(app)/clienti/page.tsx:30-39, 43-47`. Il pulsante primario compare solo nel ramo pieno e in quello vuoto; se la query fallisce resta solo Riprova. Rimandato: in quello stato Riprova è l'azione giusta, e aggiungere un secondo pulsante primario violerebbe UX-DR10. **Chiusa nella revisione della Story 1.7, il 5 agosto 2026.** Il motivo del rimando era scaduto: `ErrorState` rende `Riprova` come `btn btn--secondary` (`components/error-state.tsx:11`), quindi `Nuovo cliente` nell'intestazione non è una *seconda* primaria, è l'unica, e UX-DR10 non viene toccata. La condizione dell'intestazione è ora `error || found !== null || (filtered && !archiveEmpty)`. Resta aperta la sorella sulla card delle persone (revisione della 1.5), che è un file fuori dal perimetro di questa story.
- **Nessun `metadata` sulle rotte nuove** — `src/app/(app)/clienti/[id]/page.tsx`, `src/app/(app)/clienti/nuovo/page.tsx`, e dalla Story 3.2 anche `src/app/(app)/clienti/[id]/schede/[assessmentId]/page.tsx`. Il titolo del browser resta "Registro" per ogni cliente: con due schede aperte diventa un indovinello. Rimandato: fuori dal perimetro della story, appartiene a una passata di rifinitura. **Annotazione dalla Story 3.2, il 9 agosto 2026**: la rotta nuova è la schermata su cui si passa più tempo di tutte, quindi quella che finisce davvero in una scheda del browser lasciata aperta — due schede dello stesso cliente danno due titoli identici, e lì l'indovinello non è più teorico. La voce vale per tutte le rotte, ma l'elenco è quello che si legge, quindi la rotta ci entra.

## Deferred from: creazione della story 1-2-vedere-e-modificare-la-scheda-del-cliente (2 agosto 2026)

- **L'alone del focus usa la tinta sbagliata su tutte le schermate** — `src/app/globals.css` (`.input:focus`). `.input:focus` illumina in `--sec-prequalifica`, mentre UX-DR14 chiede la tinta della sezione corrente, che nell'area clienti è `--sec-clienti`. Emerso perché la Story 1.2 riempie una schermata di clienti di campi e rende la discordanza visibile ovunque. Rimandato per decisione di Luca del 2 agosto 2026: la correzione tocca ogni campo del progetto, `/accedi` compreso, e appartiene alla Story 5.2, passata di accessibilità verificata.

## Deferred from: code review of 1-2-vedere-e-modificare-la-scheda-del-cliente (3 agosto 2026)

- **Fra l'ultima battuta e i tre secondi non c'è protezione** — `src/app/(app)/clienti/[id]/client-field-form.tsx:124`. Chiudere la scheda o ricaricare mentre la textarea ha ancora il fuoco perde quello che è stato scritto: allo smontaggio c'è `clearTimeout` e nient'altro, nessun `beforeunload` e nessuna scrittura di chiusura. Rimandato per decisione di Luca del 3 agosto 2026: la finestra è di tre secondi e solo col fuoco ancora dentro, e il salvataggio all'uscita dal campo copre già ogni navigazione dentro l'applicazione. Da riprendere se la Story 3.3 rende la finestra più frequente, o se un `beforeunload` diventa accettabile.
- **I due campi lunghi che salvano insieme possono far tornare indietro `baseline`** — `src/app/(app)/clienti/[id]/client-field-form.tsx:38` con `src/app/(app)/clienti/actions.ts:169`. `business_goals` e `notes` hanno due `useActionState` distinti, quindi React non li mette in fila. Ognuna delle due azioni chiama `revalidatePath` sulla scheda e produce un payload reso al proprio momento: se quello partito prima arriva dopo, il campo che ha già scritto riceve un `stored` precedente alla propria scrittura, `dirty` torna vero su un campo salvato e il timer rispara lo stesso testo. Rimandato: dipende dall'ordine di consegna dei payload RSC, non è stato riprodotto, e si autocorregge al giro successivo. Da riprendere se la Story 3.3, che avrà molti più campi che salvano da soli sulla stessa schermata, lo rende frequente. **Annotazione dalla Story 3.2, il 9 agosto 2026**: il conteggio non è più un'incognita. La schermata di compilazione mette **un campo per domanda attiva** — ventiquattro oggi, contate — tutti sulla stessa pagina, e la 3.3 li farà salvare da soli tutti insieme. La voce non si muove, perché la 3.2 non salva niente: lo stato dei campi è un `useState` locale in `answer-field.tsx` e nessuna azione parte. Ma chi la riprenderà con la 3.3 sa già che il caso passa da due campi a ventiquattro, cioè che «raro» smette di essere l'argomento.

## Deferred from: code review of 1-3-cambiare-lo-stato-del-cliente (3 agosto 2026)

- **Uno stato fuori dai cinque si mostra come `potenziale` senza dirlo** — `src/app/(app)/clienti/[id]/client-field-form.tsx:127`. Il `<select>` rende solo le cinque `<option>` di `CLIENT_STATUSES`; quando `value` non corrisponde a nessuna, React seleziona la prima opzione non disabilitata. La scheda dichiara `potenziale` su un valore diverso, e siccome `value === baseline` non compare nemmeno `Salva`: il valore ignoto resta nel database senza modo di uscirne. `StatusPill` gestisce apposta lo stesso caso (`status-pill.tsx:26`), il selettore no. Rimandato: la colonna è `not null` con `check` sui cinque valori, quindi oggi il caso non esiste. Da chiudere il giorno che uno stato entra nel vincolo, insieme alla mappa della pillola.
- **`save()` non controlla `dirty`: un Invio sul selettore riscrive lo stesso stato** — `src/lib/use-editable-field.ts:144`. `save: () => { if (!pending) write(value) }`. Un `<select>` non blocca l'invio implicito del form, quindi Invio a campo non modificato arriva a `onSubmit` e scrive: il trigger sposta `updated_at`, `revalidatePath('/clienti')` riordina, e il cliente salta in cima per un tasto premuto. Rimandato: vale identico per i dieci campi corti dalla Story 1.2, e quel file è appena uscito da una revisione che la story dichiara intoccabile oltre la riga dei tipi. Da chiudere in una passata sola su tutti i campi.
- **Un `baseline` che cambia non risincronizza `value`** — `src/lib/use-editable-field.ts:60`. `useState(baseline)` inizializza una volta e nulla riallinea il valore quando la prop cambia: con la stessa scheda aperta in due posti, la scrittura dell'altro fa comparire `Salva` e `Annulla` da soli, e quel `Salva` riscrive il valore vecchio sopra il nuovo. Rimandato: preesistente dalla Story 1.2, vale per ogni campo, e con un utente solo lo scenario è raro. Parente della voce già rimandata sui due campi lunghi che salvano insieme.
- **Cinque ternari annidati nella resa del campo** — `src/app/(app)/clienti/[id]/client-field-form.tsx:108`. Il file è passato da 157 a 175 righe e resta sotto il limite, ma la catena rende ormai cinque controlli diversi con i commenti dentro i rami, e il sesto `kind` — i tag della Story 1.4 — finisce lì dentro. Rimandato: oggi il limite di `kb-0.md` §2 è rispettato e la story vieta esplicitamente un secondo componente. Da riprendere quando la Story 1.4 aggiunge il sesto ramo: lì la scelta è fra una mappa `kind → renderer` e la divisione del file. **La Story 1.4 non ha aggiunto il sesto ramo**: i tag hanno un componente proprio e questo file non è stato aperto. La voce resta com'era.

## Deferred from: code review of 1-4-marcare-i-clienti-con-tag-riusabili (3 agosto 2026)

- **`maxLength` tronca l'incolla in silenzio e rende irraggiungibile il messaggio del limite** — `src/app/(app)/clienti/[id]/client-tags-form.tsx:166`, `src/app/(app)/clienti/[id]/client-field-form.tsx:120`, `src/app/(app)/clienti/nuovo/new-client-form.tsx:50`. L'attributo `maxLength` porta lo stesso numero che il server usa come soglia di rifiuto: dall'interfaccia il limite non si supera mai, quindi il messaggio che spiega cosa fare è codice che nessuno vedrà, e quello che l'utente vede è invece un incolla tagliato senza avviso. Rimandato: il pattern esiste in tre punti con due costanti diverse (`CLIENT_NAME_MAX_LENGTH` e `CLIENT_TAG_MAX_LENGTH`), è arrivato con la Story 1.1 e si chiude in una passata sola. La strada è togliere `maxLength` e lasciare parlare il server, oppure dire a schermo quanto manca.
- **Nessuna normalizzazione Unicode, e `INVISIBLE` copre meno di quanto il commento promette** — `src/lib/client-tags.ts:29` con `src/lib/validate-client-name.ts:15`. `normalizeTag` toglie invisibili, comprime spazi e mette in minuscolo, ma non normalizza le forme Unicode: `città` in forma decomposta (`a` + U+0300, quello che arriva da molti incolla) e `città` battuto a tastiera restano due tag distinti che si leggono uguali, e il controllo dei doppioni non li vede. `INVISIBLE` è `[U+200B–U+200D, U+2060, U+FEFF]`: fuori restano U+00AD, U+180E e i controlli di direzione U+202A–U+202E, tutti invisibili e tutti frequenti nel testo incollato dal web. Rimandato: è lo stesso buco di `normalizeClientName` dalla Story 1.1, non introdotto qui, e si chiude aggiungendo `.normalize('NFC')` in un posto solo. Da riprendere insieme, perché la costante è condivisa.
- **La query dei suggerimenti non ha `.limit()` e PostgREST tronca in silenzio** — `src/app/(app)/clienti/[id]/page.tsx:75`. `supabase.from('clients').select('tags')` senza limite né ordinamento: oltre il tetto di righe di PostgREST i suggerimenti diventano incompleti e niente lo segnala, e il commento accanto giustifica il costo della lettura, non il troncamento. Rilegge anche la riga del cliente corrente, che è già in `data`. Rimandato: è lo stesso pattern di `findExistingName` (`actions.ts:353-356`), oggi i clienti sono qualche decina, e la strada vera è la migrazione con `unnest` che l'Epic 1 dichiara di non portare. Da riprendere se i clienti superano il migliaio, o insieme alla Story 1.7 che tocca comunque i tag. **La Story 1.7 non la chiude, e la allarga** (4 agosto 2026): lo stesso schema esiste ora anche in `src/app/(app)/clienti/page.tsx`, cioè sulla schermata iniziale, per costruire il selettore dei tag. Al tetto di righe di PostgREST quel selettore offre un elenco incompleto, quindi un tag che esiste diventa non filtrabile e niente lo dice — un difetto che non produce un errore, produce un filtro che non si può scegliere. La strada resta la migrazione con `unnest`.
- **Nessun aggiornamento di stato è dentro `startTransition`** — `src/app/(app)/clienti/[id]/client-tags-form.tsx:81` con `src/lib/use-editable-field.ts:88`. `startTransition(() => { void action(…).then(…) })`: la funzione di scope è sincrona, quindi solo l'invocazione dell'azione è marcata come transizione, mentre `setPending`, `setError` e `setDraft` girano in un microtask successivo, fuori dal tracciamento, come aggiornamenti urgenti. Il campo si svuota nel commit precedente a quello che porta il chip rivalidato, e su rete lenta c'è una finestra senza né il testo digitato né la conferma. Rimandato: il pattern arriva da `use-editable-field.ts` ed è stato deciso in revisione il 3 agosto 2026 per un motivo che resta valido (non passare da `useActionState`); la forma corretta è una funzione di scope `async` con `await`, e va cambiata nei due file insieme.
- **La corsa lettura-scrittura sull'array `tags`** — `src/app/(app)/clienti/actions.ts:353`+`376` e `:423`+`441`. `addClientTag` e `removeClientTag` leggono `tags`, lo ricompongono e riscrivono l'array intero: con la stessa scheda aperta in due posti, due scritture in volo insieme leggono lo stesso elenco di partenza e la seconda sovrascrive la prima. Il tag aggiunto dall'altra parte sparisce senza errore. Rimandato per decisione di Luca del 3 agosto 2026: l'alternativa è una funzione nel database che faccia `array_append` in una sola istruzione, cioè una migrazione nuova per una corsa che richiede due schede aperte sullo stesso cliente nello stesso secondo, con un utente solo (D9), mentre `AGENTS.md` dichiara che l'Epic 1 non porta migrazioni. La motivazione completa sta in `1-4-marcare-i-clienti-con-tag-riusabili.md`, sezione «Una corsa nota, e perché si accetta»; qui resta la voce perché fuori da quel file non era visibile a nessuno. Da riprendere il giorno che gli utenti diventano più di uno, insieme alle policy, che quel giorno cambiano comunque.

## Deferred from: code review of 1-5-registrare-le-persone-e-chi-decide (4 agosto 2026)

- **`PERSON_GONE` esce senza rivalidare: la riga fantasma resta a schermo e interattiva** — `src/app/(app)/clienti/people-actions.ts:151`, `:198`, `:332`. Quando la persona non c'è più, i tre rami `!data` di `updatePersonField`, `updatePersonRoles` e `deletePerson` ritornano prima di qualsiasi `revalidatePath`: il blocco resta montato con i suoi sei campi, le sue caselle e i suoi pulsanti, tutti destinati a rispondere di nuovo lo stesso messaggio. `setPersonPrimary` invece rivalida (`:280`), perché lì il `client_id` è già in mano. Rimandato: senza la riga non c'è `client_id`, e prenderlo dal `formData` è esattamente quello che la story vieta per non lasciar scegliere al browser quale scheda toccare. Il messaggio dice già «Ricarica la pagina», quindi il ripristino esiste. Da riprendere se si decide che `revalidatePath` non è un confine di sicurezza e può accettare un percorso suggerito dal client.
- **Lo stato locale dei ruoli non si risincronizza quando il dato cambia da fuori** — `src/app/(app)/clienti/[id]/person-roles-form.tsx:27`. `useState(stored)` inizializza una volta sola e solo il callback di successo riallinea: con la stessa scheda aperta in due posti, la scrittura dell'altra fa comparire `Salva` e `Annulla` da soli su caselle ferme al valore vecchio, e quel `Salva` riscrive il valore superato sopra il nuovo. Rimandato: è la stessa voce già aperta su `use-editable-field.ts:60` dalla Story 1.3, vale identica per i sei campi di testo della persona, e si chiude in una passata sola su tutti i campi invece che componente per componente.
- **Nessun limite di attesa in `useWrite`: i controlli restano spenti finché la rete non decide** — `src/lib/use-write.ts:56`. `await call().then(it => it, () => null)` intercetta il rifiuto ma non il non-arrivo: se la richiesta non si risolve né si rifiuta — passaggio wifi/cellulare su tablet, dispositivo che si sospende — il controllo resta `disabled` e muto per tutto il timeout del browser, senza errore. Sulla spunta del contatto principale, che per scelta non ha indicatore di salvataggio, il risultato è indistinguibile da un tocco non registrato. Rimandato: `use-editable-field.ts:91` ha esattamente la stessa forma dalla Story 1.2, e un `AbortSignal.timeout` va messo nei due file insieme o in nessuno.
- **Nello stato d'errore della card delle persone non resta nessun modo di aggiungerne una** — `src/app/(app)/clienti/[id]/people-card.tsx:52-57`. Con `people === null` il conteggio vale 0, quindi né il pulsante dell'intestazione né quello dello stato vuoto si rendono: la card è un riquadro d'errore e basta, e `Riprova` è un `<Link>` verso la rotta su cui si è già. Rimandato: è la stessa forma della voce già aperta sull'elenco clienti («nel ramo d'errore non resta nessun modo di creare un cliente»), il comportamento di `ErrorState` è quello di tutto il progetto, e in quello stato `Riprova` è l'azione giusta. Da chiudere insieme all'altra, quando si decide cosa fa davvero `Riprova` su una rotta già aperta. **La Story 3.1 la estende** (9 agosto 2026): `src/app/(app)/clienti/[id]/assessments-card.tsx` ha la stessa forma riga per riga, quindi con `assessments === null` non resta nessun modo di aprire una scheda. Sono ora tre card con lo stesso comportamento, e si chiudono in una decisione sola.
- **Nessun limite di lunghezza sui sei campi della persona** — `src/app/(app)/clienti/people-actions.ts:77` e `:132`. `normalizeTextValue` fa solo `trim()` e «stringa vuota → `null`»: nessun `maxLength` sugli input, nessun tetto nell'azione, sei colonne `text` che accettano una richiesta di dimensione arbitraria. Task 1 della story lo dichiara voluto in nome di D14, e la domanda 6 lo conferma, ma D14 parla di non vietare azioni, non di non porre limiti tecnici: `name` ha 200 caratteri e i tag 40, e `kb-0.md` §3 chiede che ogni input si validi sul server con uno schema. Rimandato: è la stessa passata della voce sul `maxLength` che tronca in silenzio, e le due si chiudono insieme decidendo una volta sola dove sta il limite e chi lo dice a schermo.

## Deferred from: code review of 1-6-ordinare-l-elenco-per-ultima-attivita-vera (4 agosto 2026)

- **Innesto e clienti senza `.limit()`: PostgREST tronca in silenzio** — `src/app/(app)/clienti/page.tsx:33`. La `select` non limita né i clienti né le schede innestate. Al tetto di righe di PostgREST la finestra viene scelta ordinando per `clients.updated_at`, mentre la chiave d'ordine vera è l'ultima attività: un cliente recente solo per una scheda viene tagliato prima di essere ordinato, e l'elenco esce in un ordine sbagliato ma plausibile. È la stessa categoria della voce sui suggerimenti dei tag, e la stessa che AC4 dichiarava di voler proteggere. L'ottimizzazione che l'avrebbe contenuta — `.limit(1, { referencedTable: 'assessments' })` — è stata scartata perché non verificabile su una tabella vuota. Da riprendere quando le schede esistono, o al migliaio di clienti, qualunque arrivi prima. **La Story 3.1 rimuove l'impedimento** (9 agosto 2026): `assessments` non è più vuota, quindi il comportamento dell'innesto con `.limit()` si può misurare invece che ipotizzare. Il rimando resta — la voce parla del troncamento al tetto di righe, che è un altro ordine di grandezza — ma la ragione «non verificabile» è scaduta.
- ~~**L'elenco clienti dipende da una tabella mai interrogata in produzione**~~ — `src/app/(app)/clienti/page.tsx:33`. **Chiusa il 4 agosto 2026**, con la verifica di sessione di Task 6 della story 1.6: la query con l'innesto è stata eseguita con una sessione vera e ha risposto, e nessun cliente è sparito dall'elenco. Resta vero che l'innesto sta nella stessa richiesta, quindi un problema futuro su `assessments` farebbe cadere l'intera schermata iniziale sullo stato d'errore invece di darla con una colonna in meno: da riguardare quando le schede esisteranno davvero.
- **`npm test` non è attraversato da nessun cancello** — niente `.github/`, niente `.husky/`, e `next build` non esegue i test. Il comando esiste (AC4 chiedeva quello) ma i due guardiani appena scritti proteggono solo chi si ricorda di invocarli a mano. `kb-0.md` §10 vuole che si rilasci da un branch passato dall'anteprima: oggi un test rosso non ferma nessun rilascio.
- **`allowImportingTsExtensions` allenta tutto il progetto per due file di test** — `tsconfig.json:12`. Rende legale `import … from './foo.ts'` ovunque sotto `**/*.ts`, codice di rotta compreso, e non c'è nessun linter nel progetto che lo impedisca. Una regola di compilazione globale cambiata per una comodità locale. **La Story 1.7 la allarga** (5 agosto 2026): `src/lib/client-tags.ts` e `src/lib/client-filters.ts` usano ora `.ts` sui propri import e stanno **nel grafo dell'app**, non solo in quello dei test, quindi non si tratta più di «due file di test». La regola vera — un modulo di `src/lib` raggiunto da un test deve usare `.ts` su tutta la propria catena, altrimenti il test muore con `ERR_MODULE_NOT_FOUND` da un file che chi lo scrive non ha toccato — non è scritta in nessun file di istruzioni, e né `tsc --noEmit` né `next build` possono segnalarla. In revisione è stata messa come commento su `client-tags.ts:1`, che è il posto dove la trova chi inciampa; il posto giusto se la regola si conferma è `AGENTS.md`, e quel file è di Luca.
- **`lastActivityAt` ricalcolato due volte per confronto, più una per riga in resa** — `src/lib/last-activity.ts:47`. Il comparatore riscorre le schede e rifà `Date.parse` a ogni confronto. Decorare una volta e ordinare sui numeri era la forma ovvia. Oggi trascurabile, perché nessuna scheda esiste; conta perché la story 1.6 motiva l'intera scelta architetturale col costo dell'ordinamento in memoria.
- ~~**La revalidazione di `/clienti` quando cambia una scheda**~~ — da oggi l'ordine dell'elenco dipende anche da `assessments.updated_at`, ma nessuna azione rivalida `/clienti`: `revalidatePath('/clienti')` sta solo nelle azioni del cliente. Il giorno in cui la Story 3.3 salva una risposta, quella scrittura deve rivalidare anche l'elenco, altrimenti si lavora un'ora su una scheda, si torna all'elenco e il cliente è dov'era. **Chiusa dalla Story 3.1, il 9 agosto 2026**: la prima scrittura su `assessments` esiste — `openAssessment` in `src/app/(app)/clienti/assessment-actions.ts` — e rivalida sia la scheda del cliente sia `/clienti`, con il motivo scritto accanto alla seconda chiamata. La regola vale da qui in avanti per ogni scrittura che tocchi una scheda, la 3.3 compresa.

## Deferred from: code review of 1-7-cercare-e-filtrare-l-elenco (5 agosto 2026)

- **Le due letture della schermata iniziale sono attese in serie invece che in parallelo** — `src/app/(app)/clienti/page.tsx:68` e `:75`. `await query.order(…)` e `await supabase.from('clients').select('tags')` stanno una dopo l'altra, ma non si scambiano niente: nessun parametro della seconda dipende dal risultato della prima. La latenza del database si somma invece di sovrapporsi, e succede sulla schermata che si apre più spesso. Un `Promise.all` non cambia nessun'altra riga. Rimandato: `AGENTS.md` vieta il refactor non concordato, e la story non chiede prestazioni. Da chiudere quando si tocca comunque quel blocco, o se la schermata iniziale diventa lenta.

## Deferred from: code review of 1-8-poter-tornare-indietro-da-una-modifica-di-schema (5 agosto 2026)

- ~~**`database.md` §8 contraddice il commento nuovo di `0004`**~~ — `docs/database.md` §8 diceva che «il seed crea il questionario vuoto e le domande si inseriscono dall'interfaccia», mentre la dichiarazione aggiunta a `supabase/migrations/0004_questionnaire.sql` dice che il seed rimette le 23 domande approvate. Contate: `supabase/seed.sql` ne inserisce davvero 23 in 8 blocchi, quindi il commento nuovo ha ragione e §8 era rimasta indietro. Rimandato: il perimetro della Story 1.8 limitava `database.md` alla §7, e riscrivere §8 senza averla in perimetro sarebbe stata deriva del piano. **Chiusa dalla Story 2.1, il 5 agosto 2026**, che è la prima story dell'Epic 2 e legge quelle tre tabelle per la prima volta. §8 ora dice quello che il seed fa davvero — 8 blocchi e 23 domande, rieseguibile, da eseguire dopo il primo accesso — e la parte sul «finché le domande non sono state riviste» è caduta perché la revisione è la decisione D19. Nello stesso passaggio §8 ha preso anche la nota sulle posizioni globali, che era la scoperta della Story 2.1 e non aveva un posto dove si cerca.
- **Nessuna versione di Node dichiarata nel progetto** — `package.json` non ha `engines`, e non esiste nessun `.nvmrc`. `supabase/migrations.test.ts` usa `import.meta.dirname` (Node ≥ 20.11) e l'intero comando `test` esegue `.ts` direttamente da `node --test` (≥ 22.6): due soglie reali che nessun file dichiara. La versione verificata, `v24.13.0`, è scritta solo dentro i file delle story, che nessuno strumento legge. Su una macchina o una CI con Node più vecchio l'errore è criptico e sembra un guasto del test invece che una versione sbagliata. Rimandato: pre-esistente dalla Story 1.6, che ha introdotto l'esecuzione dei test in TypeScript; la Story 1.8 ha solo alzato di poco la soglia. Da chiudere insieme alla voce sul cancello che manca a `npm test`, perché è la stessa passata: chi mette il cancello deve comunque dire su quale Node gira.
- **`readdirSync` non è ricorsivo e filtra solo `.sql` minuscolo** — `supabase/migrations.test.ts:39-41`. Una migrazione dentro una sottocartella di `supabase/migrations/`, o chiamata `0008_x.SQL`, non viene mai letta: il controllo non la vede e non produce nessun rosso, cioè esattamente il difetto che esiste per intercettare. Rimandato: oggi la cartella è piatta e la Story 1.8 vieta esplicitamente strutture nuove, rinomine e rinumerazioni, quindi non c'è nessuna istanza. Da riprendere il giorno che la cartella smette di essere piatta — se le migrazioni diventano tante e si archiviano per anno, quel giorno il controllo va reso ricorsivo nella stessa modifica, non dopo.

## Deferred from: code review of 2-1-vedere-il-questionario-com-e-oggi (6 agosto 2026)

- **Il messaggio d'errore diagnostica, e diagnostica male il guasto più probabile di quella query** — `src/app/(app)/questionario/page.tsx:86`. «La connessione al database non ha risposto» dichiara una causa che il codice non conosce: la cosa nuova in quella lettura è la sintassi `referencedTable: 'question_blocks.questions'`, e se smettesse di essere accettata la risposta sarebbe un `PGRST100` o un `42703` — il database avrebbe risposto benissimo, e l'utente leggerebbe il contrario. `kb-0.md` §3 chiede un messaggio che dica *cosa fare*. Rimandato: è replicato alla lettera da `clienti/page.tsx:149`, quindi è pre-esistente e vale su due schermate; si chiude in una passata sola, insieme alla voce già aperta su cosa faccia davvero un `Riprova` verso la rotta su cui si è già.
- **AC3 regge nello stato pieno e non in quello d'errore** — `src/app/globals.css:83-87`. Il ramo d'errore monta `ErrorState`, il cui `Riprova` è focalizzabile *dentro il contenuto*, e `:focus-visible` lo illumina in `--sec-clienti-tint`: un colore di sezione, per giunta di un'altra sezione, dentro il contenuto della schermata del questionario. Nello stato pieno la pagina non ha niente di focalizzabile, quindi la verifica «dentro la pagina non c'è niente di viola» non tocca mai il caso in cui la regola si rompe. Rimandato: è la voce `:focus-visible` già aperta dal 2 agosto e già assegnata alla Story 5.2, vale per ogni campo del progetto, e la Story 2.1 la dichiara esplicitamente fuori perimetro.
- **Un questionario con `is_active = false` si mostra come «non ancora caricato», con un'istruzione che non fa niente** — `src/app/(app)/questionario/page.tsx:55` e `:96-99`. La pagina filtra su `is_active = true`, zero righe portano al ramo vuoto, e la frase dice di eseguire `supabase/seed.sql`; il seed si ferma su `exists (select 1 from questionnaires where owner_id = v_owner)` (`seed.sql:26`), che **non** guarda `is_active`, quindi risponde «Questionario già presente» e torna. Il questionario con le sue 23 domande esiste, è invisibile, e la schermata prescrive un'operazione nulla: è un terzo stato collassato nel vuoto, la stessa forma di bugia che il codice evita con cura fra errore e vuoto. Rimandato: raggiungibile solo da SQL, perché nessun codice scrive `questionnaires`. Da riprendere appena l'Epic 2 tocca `questionnaires.is_active`. **Annotazione dalla Story 2.6, l'8 agosto 2026**: l'Epic 2 si è chiusa senza che nessun codice scriva `questionnaires.is_active` — la 2.6 disattiva le *domande*, non il questionario. Il punto di ripresa resta «appena qualcosa scrive `questionnaires`», invariato.
- ~~**`questions.text` e `question_blocks.title` non hanno il `check (length(trim(…)) > 0)` che `clients.name` ha**~~ — `supabase/migrations/0004_questionnaire.sql:31,39`. Sono `not null` ma accettano la stringa vuota: una domanda diventerebbe una riga anonima con solo il badge sotto, un blocco una card con l'intestazione vuota, e niente a schermo direbbe che il campo è vuoto. Rimandato: è una differenza di schema pre-esistente e la Story 2.1 non tocca lo schema. Da chiudere nella story che introduce la scrittura — 2.2 per il titolo, 2.3 e 2.4 per il testo — perché è lì che la validazione lato server va scritta comunque, e `kb-0.md` §5 vuole il vincolo nel database e non solo nel codice. **La Story 2.2 la chiude a metà, il 7 agosto 2026**: `question_blocks.title` ha ora il vincolo `question_blocks_title_not_blank` (`supabase/migrations/0008_block_title_check.sql`) e la validazione server in `validateBlockTitle` (`src/lib/block-title.ts`). **Chiusa del tutto dalla Story 2.3, il 7 agosto 2026**: `questions.text` ha il vincolo `questions_text_not_blank` (`supabase/migrations/0009_question_checks.sql`, che porta anche `questions_single_choice_has_options` per AC2) e la validazione server in `validateQuestionText` (`src/lib/question-fields.ts`). La 2.4, che riscrive invece di creare, riusa lo stesso modulo e lo stesso vincolo: non resta niente da introdurre.
- **Gli scheletri combaciano su una sola forma di card e a una sola larghezza** — `src/app/(app)/questionario/loading.tsx:47-49`. Il «salto 0» è stato misurato a 1280 contro il blocco 1, i cui tre testi di aiuto stanno tutti su una riga; le altre due card dello scheletro stanno al posto di blocchi il cui aiuto arriva a 159 caratteri, e ogni riga in più vale 16px che il rettangolo ad altezza fissa non ha. A 375px lo spazio di testo dentro la card scende a circa 295px e il salto torna, massimo sul dispositivo peggiore. Rimandato: la misura vera è dentro Task 7 della story, che richiede una sessione e resta a Luca; questa voce esiste perché la conclusione «salto 0» non sia letta come valida a ogni larghezza.
- **La fascia di navigazione su schermo stretto cresce con le voci** — `src/app/globals.css:125-129` e `:917-930`. `.nav` è `flex-direction: column` e l'unica regola responsive porta `.shell` a una colonna: le voci si impilano, e con due voci sono 44px + 4 di gap prima di qualsiasi contenuto. `design-system.md` §2 elenca già cinque sezioni, quindi a regime sono oltre 220px di navigazione sopra la prima domanda. Rimandato: è la Story 5.1, navigazione laterale richiudibile, ed è il suo problema per definizione. Qui si registra solo che la fascia in alto ha smesso di essere invariata: prima conteneva una voce sola.

## Deferred from: implementazione della story 2-2-aggiungere-e-rinominare-un-blocco (7 agosto 2026)

Le due voci vengono dalle domande 1 e 2 della story, chiuse da Luca il 7 agosto 2026 con il default proposto. Stanno qui perché fuori dal file della story nessuno le troverebbe.

- **`question_blocks` non ha `updated_at` né trigger, e da questa story un blocco rinominato non sa dire quando** — `supabase/migrations/0004_questionnaire.sql:28-34`, `0006_triggers.sql`. È l'unica delle sette tabelle senza, e D10 chiede che ogni record porti la data dell'ultima modifica per le esportazioni. Rimandato per decisione di Luca del 7 agosto 2026: oggi nessuno leggerebbe quella colonna, e un blocco non esce da sé — nell'esportazione compare come `answers.block_title`, che è una copia con il suo `updated_at`. Aggiungere colonna e trigger che nessuno legge è il campo «utile un giorno» che `kb-0.md` §4 chiede di non aggiungere. Da riprendere quando le esportazioni della terza versione fanno uscire i blocchi come entità proprie. **Dalla Story 2.5, l'8 agosto 2026, il blocco è anche mobile**: `move_block` (0011) ne riscrive `position`, e nemmeno uno spostamento sa dire quando. Stesso rimando.
- ~~**`questionnaires.version` resta `1`: nessuna delle scritture dell'Epic 2 la fa crescere**~~ — `docs/database.md` §3 dice che cresce «quando si modifica la struttura», ma nessun codice la scrive, e `assessments.questionnaire_version` (Story 3.1) registrerebbe sempre lo stesso numero. Rimandato per decisione di Luca del 7 agosto 2026, con il punto di chiusura dichiarato: **la fine dell'Epic 2**, quando esistono tutte e cinque le scritture e si decide una volta sola che cosa conta come modifica strutturale. Farla crescere story per story sarebbe una seconda scrittura senza transazione — PostgREST non ne offre una — cioè uno stato incoerente raggiungibile per un numero che oggi nessuno legge. Scritto anche dove si cerca, in `database.md` §3. **Chiusa dalla Story 2.6, l'8 agosto 2026, con la decisione D25**: l'Epic 2 è finita, le cinque scritture esistono, e `version` resta `1` — nessuna la muove, finché nessuno la legge. La ricostruzione sta nelle copie dentro `answers`, la colonna resta per le esportazioni future, e la 3.1 vi copierà `1`. L'esito per esteso sta in `00-contesto-e-decisioni.md` (D25) e in `database.md` §3.

## Deferred from: code review of 2-2-aggiungere-e-rinominare-un-blocco (7 agosto 2026)

- **La corsa lettura-scrittura sulla posizione dei blocchi** — `src/app/(app)/questionario/actions.ts:82-104`. `createBlock` legge l'ultima posizione e inserisce in due istruzioni separate: due aggiunte in volo insieme leggono la stessa «ultima» e nascono con la stessa `position`. Nessun indice unico lo impedisce, di proposito: la Story 2.5 dovrà riscrivere le posizioni in blocco e un vincolo unico non differibile la bloccherebbe a metà riordino. L'ordine a schermo resta stabile (`page.tsx` ordina su `position, created_at, id`); si perde solo la promessa delle posizioni consecutive. Accettata come da Dev Notes della story, con lo stesso ragionamento della corsa sull'array dei tag (decisione di Luca del 3 agosto 2026): richiede due schede aperte nello stesso secondo con un utente solo (D9), e l'alternativa è una funzione nel database, cioè una migrazione in più. Da riprendere con la Story 2.5, che decide cosa vuol dire «posizioni consecutive», o il giorno che gli utenti diventano più di uno. **Estesa dalla Story 2.3, il 7 agosto 2026, a `createQuestion`** (`question-actions.ts:122-147`), dove la corsa è più larga: il massimo è globale al questionario, quindi due aggiunte in volo collidono anche su blocchi *diversi*. Stessa accettazione e stesse ragioni; `answers.position` copierà questi numeri solo con la 3.1, che nello sprint viene dopo la rinumerazione della 2.5. **La Story 2.5 chiude la definizione, l'8 agosto 2026**: «posizioni consecutive» è la postcondizione di ogni scrittura di riordino e di ogni creazione — `renumber_questions` (0011) riassegna 1..N in transazione dopo ogni `move_*` e dopo ogni `createQuestion` — quindi un duplicato nato dalla corsa si sana alla prima rinumerazione. La corsa sull'*insert* in sé resta (due istruzioni senza transazione), con la stessa accettazione: il suo danno è ora temporaneo per costruzione. **Precisazione dalla revisione della 2.5, l'8 agosto 2026**: la sanatura vale per le *domande* — `renumber_questions` riscrive solo `questions.position`; le posizioni dei *blocchi* si riscrivono solo dentro `move_block`, quindi un duplicato fra blocchi persiste finché un altro spostamento di blocco non riesce, con danno nullo (l'ordine a schermo regge sui pareggi `created_at, id`). Dalla 0012, in più, ogni scrittura di posizione prende un lock consultivo sul questionario: due riordini in volo si mettono in coda invece di mescolarsi, e resta solo la corsa sull'insert, la stessa di sempre.
- ~~**`apply_migration` registra il nome senza il prefisso numerico, e il rimedio vive solo in un Dev Agent Record**~~ — la 0008 è stata registrata come `block_title_check` invece di `0008_block_title_check`, e il registro è stato allineato con un `update` manuale su `supabase_migrations.schema_migrations` (metadato dello strumento, non schema applicativo). Capiterà identico dalla 0009 in avanti, e fuori dal Dev Agent Record della story 2.2 nessuno lo troverebbe. Da chiudere alla prossima migrazione: o si passa il nome con il prefisso allo strumento in modo che lo conservi, o la verifica «`list_migrations` risponde N righe con il prefisso in fondo» entra nel task di ogni migrazione come passo dichiarato, con l'allineamento del registro come rimedio noto. **Chiusa dalla Story 2.3, il 7 agosto 2026**, con tutte e due le strade: la 0009 è stata applicata passando `0009_question_checks` con il prefisso, e lo strumento l'ha conservato (`list_migrations` risponde nove righe, prefisso compreso); il passo di verifica con il rimedio dell'allineamento è ora scritto dove si cercano le migrazioni, in `database.md` §7.
- **Creando il primo blocco dallo stato vuoto, il fuoco della tastiera cade sul body** — `src/app/(app)/questionario/page.tsx:123-147` con `src/app/(app)/questionario/new-block-form.tsx:25-34`. I due punti di montaggio di `NewBlockForm` sono due istanze diverse: dopo il primo `createBlock` la rivalidazione cambia ramo, l'istanza dello stato vuoto si smonta con il suo `ref`, e il ritorno di fuoco su `Aggiungi blocco` non ha dove atterrare. Solo da tastiera, solo il primo blocco in assoluto — e la sessione di Task 7 parte da otto blocchi, quindi non lo incontra. Rimandato: il rimedio toccherebbe la struttura a due rami che la story prescrive, per un caso che capita una volta nella vita del software. Da riprendere nella Story 5.2, la passata di accessibilità verificata, insieme alle altre voci sul fuoco.
- **L'input disabilitato durante `pending` scarica il fuoco sul body: su un rifiuto la posizione di tabulazione è persa** — `src/app/(app)/questionario/new-block-form.tsx:85`, ereditato alla lettera da `new-person-form.tsx:77-87`. Premendo Invio l'input si disabilita e il browser toglie il fuoco; se il server rifiuta, `role="alert"` annuncia l'errore ma chi naviga da tastiera riparte dal body. Rimandato: pattern pre-esistente in due moduli, e i due moduli nuovi della 2.2 rispondono diversamente allo stesso gesto (`block-title-form` non disabilita l'input). Si chiude in una passata sola su tutti i moduli «un pulsante scopre un modulo», decidendo una volta dove va il fuoco dopo un rifiuto. Anche questa appartiene alla Story 5.2. **Terza istanza dalla Story 2.3, il 7 agosto 2026**: `new-question-form.tsx` disabilita tutti i campi durante `pending` (`:102`), stesso pattern, stessa passata. **Quarta istanza dalla Story 2.4, il 7 agosto 2026**: `edit-question-form.tsx` disabilita campi e pulsanti durante `pending` (`:107`, `:123`, `:138`, `:180`, `:197`, `:208`), stesso pattern, stessa passata. **Quinta istanza dalla Story 2.5, l'8 agosto 2026, con una forma nuova**: le frecce di `move-buttons.tsx` si spengono durante `pending` e al bordo, quindi un elemento spostato fino in cima o in fondo disabilita il pulsante appena premuto e il fuoco cade sul body. Stessa passata della 5.2. **Sesta istanza dalla Story 2.6, l'8 agosto 2026**: `question-active-button.tsx` spegne il pulsante durante il volo; al successo il pulsante si riaccende con l'etichetta girata, ma il fuoco intanto è caduto sul body. Stessa passata della 5.2.
- **`createBlock` accetta un questionario proprio ma con `is_active = false`** — `src/app/(app)/questionario/actions.ts:61-65` contro il filtro `is_active = true` di `page.tsx`. Il controllo di esistenza seleziona per solo `id`: una richiesta forgiata (o un futuro secondo questionario) scriverebbe il blocco su un questionario che la pagina non mostra mai — successo silenzioso su un contenitore invisibile. Rimandato: oggi irraggiungibile, nessun codice scrive `questionnaires` e il questionario è uno e attivo. Da riprendere appena l'Epic 2 tocca `questionnaires.is_active`, insieme alla voce gemella della revisione 2.1 sul questionario disattivato mostrato come «non ancora caricato». **Estesa dalla Story 2.3, il 7 agosto 2026, a `createQuestion`**: la lettura di controllo seleziona `id, questionnaire_id` senza guardare `is_active` (`question-actions.ts:96-110`). Stessa forma, stesso rimando. **Estesa dalla Story 2.4, il 7 agosto 2026, a `updateQuestion`** (`question-update-actions.ts`): l'azione aggiorna per solo `id` e non guarda il questionario, quindi una richiesta forgiata modificherebbe una domanda di un questionario che la pagina non mostra. Stessa classe, stesso rimando. **Estesa dalla revisione della Story 2.5, l'8 agosto 2026, a `move_block` e `move_question`** (`0011_reorder_functions.sql`, corpi dalla 0012): le funzioni risalgono al blocco o alla domanda per solo `id` senza guardare `is_active` del questionario — una richiesta forgiata riordinerebbe un questionario che la pagina non mostra mai. Stessa classe, stesso rimando. **Estesa dalla revisione della Story 2.6, il 9 agosto 2026, a `setQuestionActive`** (`question-active-actions.ts:68-73`): l'update scrive `is_active` per solo `id`, senza guardare il questionario. È la sesta e ultima istanza dell'Epic 2, e con lei l'elenco delle scritture da sanare è completo — chi riprenderà la voce le trova tutte qui. Stessa classe, stesso rimando.

## Deferred from: code review of 2-3-aggiungere-una-domanda-scegliendo-il-tipo-di-risposta (7 agosto 2026)

- **`questions_single_choice_has_options` conta gli elementi, non la sostanza** — `supabase/migrations/0009_question_checks.sql:24-25`. `coalesce(array_length(options, 1), 0) > 0` accetta `array['']`: una scelta singola con sole opzioni bianche passa l'ultima difesa. Oggi raggiungibile solo da SQL o richiesta forgiata, perché `parseOptions` non emette mai stringhe vuote. Rimandato: correggerlo ora sarebbe una migrazione (0010) per un caso non provocabile dall'interfaccia. Da riprendere se una scrittura futura di opzioni passa da un percorso diverso da `parseOptions` — la 2.4 compresa, che quel percorso lo riusa. **La Story 2.4 conferma il rimando, il 7 agosto 2026**: `updateQuestion` riusa `parseOptions`, quindi il motivo regge, e la sua migrazione — la 0010, `questions_options_only_single_choice`, la direzione inversa — di proposito non tocca questo vincolo: riscriverlo sarebbe stata una seconda migrazione dentro la stessa, per un caso non provocabile dall'interfaccia.
- **Gli invisibili fuori dall'insieme `INVISIBLE` passano tutte e due le difese** — `src/lib/question-fields.ts:2` con `0009_question_checks.sql:22-23`. `INVISIBLE` copre U+200B–U+200D, U+2060 e U+FEFF; marcatori come U+200E/U+200F o U+00AD non sono whitespace né per il `trim()` di JavaScript né per quello di Postgres: un testo composto solo da quei caratteri produce la riga anonima che il vincolo esiste per impedire. Rimandato: classe pre-esistente ereditata da `validate-client-name.ts`, identica per nomi cliente, titoli di blocco e domande; si chiude in una passata sola — ampliando l'insieme o passando a `\p{Cf}` — non story per story.

## Deferred from: code review of 2-4-riscrivere-una-domanda (8 agosto 2026)

- **Aggiornamento perso: `updateQuestion` scrive per solo `id`, senza guardia di concorrenza** — `src/app/(app)/questionario/question-update-actions.ts:86-91` con il prefill congelato al montaggio in `edit-question-form.tsx`. La stessa domanda aperta in modifica in due schede: il salvataggio più tardo sovrascrive in silenzio il più recente con valori vecchi, senza messaggio in nessuna delle due. Classe diversa dalle corse sulla posizione già registrate qui sopra: quelle duplicano un numero, questa cancella contenuto. Accettata (decisione di Luca, revisione dell'8 agosto 2026) con le stesse ragioni delle sorelle: un utente solo (D9), servono due schede sulla stessa domanda nello stesso momento, e la guardia costerebbe un `updated_at` passato al modulo, un `.eq` in più e un caso d'errore nuovo. Da riprendere quando gli utenti diventano più di uno.
- **Ventitré «Modifica» con lo stesso nome accessibile, e il modulo aperto non nomina la domanda** — `src/app/(app)/questionario/question-item.tsx:49-56` e `edit-question-form.tsx`, che non ha intestazione né nome accessibile. Chi naviga per elenco di pulsanti sente ventitré «Modifica» indistinguibili; aperto il modulo, il display sparisce e nulla oltre al contenuto della textarea dice quale domanda si sta modificando. Classe accessibilità della Story 5.2, insieme alle voci sul fuoco e ai campi non annunciati. **Estesa dalla Story 2.5, l'8 agosto 2026**: `Sposta su` e `Sposta giù` (`move-buttons.tsx`) aggiungono due pulsanti per domanda e due per blocco con nomi accessibili identici — a pagina piena sono 62 frecce indistinguibili per chi naviga per elenco. Stessa classe, stessa passata. **Estesa dalla Story 2.6, l'8 agosto 2026**: `Disattiva`/`Riattiva` (`question-active-button.tsx`) aggiunge un pulsante per domanda — fino a 23 con lo stesso nome, perché l'etichetta varia con lo stato, non con la domanda — e il modulo di modifica resta senza nome accessibile. Stessa classe, stessa Story 5.2.
- **Il giro prefill → `parseOptions` non è fedele per opzioni di forma producibile solo da SQL** — `edit-question-form.tsx:36` (`join('\n')`) contro `parseOptions` (`question-fields.ts`, `split('\n')` e scarto delle righe vuote). Un'opzione con un `\n` letterale dentro diventa due opzioni a un semplice apri-e-risalva; una di soli invisibili sparisce. Solo da SQL o richiesta forgiata: `parseOptions` non emette mai quelle forme. Stessa classe «solo da SQL» delle voci su `array['']` e sugli invisibili; si chiude con loro, non story per story.
- **Il campo opzioni che compare e scompare col tipo non è annunciato** — `src/app/(app)/questionario/new-question-form.tsx:145-153`. Nessun `aria-live` e nessuna relazione dichiarata fra il select e il campo: chi usa uno screen reader sceglie `scelta singola` senza sapere che più sotto è comparso un campo di fatto obbligatorio. Rimandato: classe accessibilità della Story 5.2, insieme alle voci sul fuoco. **Seconda istanza dalla revisione della Story 2.4, l'8 agosto 2026**: `edit-question-form.tsx:169-185` replica il pattern nel modulo di modifica, e con lui la riga d'avviso sulla perdita delle opzioni introdotta dalla stessa revisione (`:156-163`), anch'essa non annunciata. Stesso pattern, stessa passata.

## Deferred from: code review of 2-5-riordinare-blocchi-e-domande (8 agosto 2026)

- **`move_question` muove `updated_at` anche sulle domande che finiscono dov'erano** — `0012_reorder_guards.sql` (disegno dalla 0011): lo spostamento scrive due volte nella stessa transazione — gli ordinali di blocco, poi la rinumerazione globale — e una domanda che parte e arriva alla stessa posizione subisce comunque i due update: il suo `updated_at` si muove senza modifica reale. La nota della story («il trigger scatta solo su chi si è mosso davvero») vale per `renumber_questions` chiamata da sola, cioè per la creazione. Per D10 una sincronizzazione incrementale futura rispedirebbe righe invariate: costo di banda, non di correttezza. Da riprendere se le esportazioni della terza versione useranno `updated_at` come cursore.
- ~~**Riga sparita fra le due letture di `move_block`/`move_question`: eccezione da subscript `NULL` e messaggio sbagliato**~~ — `supabase/migrations/0011_reorder_functions.sql:82-90` e `:140-148`. Le funzioni sono VOLATILE in READ COMMITTED: se la riga sparisce fra la `select … into` (che ha risposto `found`) e l'`array_agg`, `array_position` risponde `NULL`, il controllo di bordo con `NULL` non scatta e `v_ids[NULL] := …` alza «array subscript in assignment must not be null» — all'utente arriva «Riprova fra un momento» per una cosa che non riuscirà mai più. Raggiungibile solo da SQL manuale, perché nessun percorso di cancellazione fisica esiste né è previsto (le domande si disattivano, `kb-0.md`; i blocchi non si eliminano). **Chiusa dalla stessa revisione, l'8 agosto 2026**: la 0012, nata per la direzione null e per il lock, ha portato anche la guardia `v_index is null` → `return false`, lo stesso `false` di «non mio o sparito».
- **L'errore delle frecce sparisce se l'intestazione entra in rinomina o modifica** — `src/app/(app)/questionario/block-title-form.tsx:80-97` (il ramo `renaming` non rende `actions`) e `question-item.tsx:46-76` (idem con `editing`): `MoveButtons` si smonta con il suo `.field__error` e alla chiusura del modulo il messaggio non ricompare. Chi preme una freccia, apre subito `Rinomina` e riceve un rifiuto non lo saprà mai — mitigato dal fatto che la rivalidazione mostra comunque l'ordine vero. Rimandato: fallimento raro, transitorio e a danno nullo; si decide una volta sola nella passata sui moduli della Story 5.2, insieme alle voci sul fuoco. **Estesa dalla revisione della Story 2.6, il 9 agosto 2026, a `QuestionActiveButton`** (`question-active-button.tsx:52-56`, montato in `question-item.tsx:78`): sta nello stesso ramo che `editing` smonta, quindi chi preme `Disattiva`, apre subito `Modifica` e riceve un rifiuto non lo saprà mai. Mitigazione identica e più forte: alla chiusura del modulo il display dice `attiva` e il pulsante `Disattiva`, cioè lo stato vero — manca solo la riga che spiega perché. Stessa passata della 5.2.

## Deferred from: code review of 2-6-disattivare-una-domanda-invece-di-cancellarla (9 agosto 2026)

- **Le etichette del modulo di modifica ereditano `--ink-muted` dentro una domanda spenta, e il commento della regola non lo dice più** — `src/app/globals.css:891-897` con `question-item.tsx:46-48` e `edit-question-form.tsx:93,113,129,171`. `.question--inactive` dichiara `color: var(--ink-muted)` sulla `<li>`, e `.label` (`globals.css:496-501`) non dichiara un colore proprio: aprendo `Modifica` su una domanda spenta — percorso che esiste dalla 2.6 e prima era raggiungibile solo da SQL — le quattro etichette si rendono smorzate, mentre campi, select, pulsanti e riga d'errore restano pieni perché il colore ce l'hanno. Rimandato: la resa non esce dal design system — `design-system.md` §2 assegna `--ink-muted` proprio a «testo secondario, etichette» e il contrasto su `--surface` resta circa 5,5:1, sopra il minimo di §8 — e la 2.6 dichiara `globals.css` invariato. Quello che è diventato falso è il commento a `:891-894` («muove solo `.question__text`, l'unica cosa qui dentro a non dichiarare un colore proprio»): valeva per il solo ramo display, e la 2.4 ha messo dentro quella `<li>` anche il modulo. Da riprendere nella prima story che apre `globals.css` per il questionario: si dichiara `color: var(--ink)` su `.label` oppure si corregge il commento, ma la scelta si fa una volta sola.
- **Due `btn--primary` neri possono stare a schermo insieme, e nessuna story dopo la 2.6 guarda più la gerarchia dei pulsanti del questionario** — `new-question-form.tsx:185`, `edit-question-form.tsx:197`, `block-title-form.tsx:135`, `new-block-form.tsx`. Lo stato `adding`/`editing`/`renaming` è locale a ogni istanza e niente lo coordina: aprire `Aggiungi domanda` su due blocchi, o due `Modifica` su due domande, lascia due pulsanti primari accesi contemporaneamente, contro UX-DR10 («una sola azione primaria per schermata»). Pre-esistente dalle Story 2.2-2.4, non introdotto dalla 2.6, che aggiunge un `btn--quiet`. Registrato qui perché `epics.md:156` assegna UX-DR10 all'Epic 2 proprio con la 2.6 — per la metà «assenza del distruttivo», che è soddisfatta e verificata col grep di AC4 — e dopo di lei nessuna story torna sulla gerarchia dei pulsanti di questa schermata: la 5.2 è accessibilità. Da riprendere nella prima passata che tocca i moduli del questionario.
- **Lo scheletro di caricamento non conosce la riga delle azioni, che ora ha quattro pulsanti** — `src/app/(app)/questionario/loading.tsx:40-45` contro `question-item.tsx:57-79` e `globals.css:841-846`. Lo scheletro disegna la domanda come tre barre impilate (24 + 16 + 22) e non riproduce `.question__header`, che è una riga flex contenente `Sposta su`, `Sposta giù`, `Modifica` e ora `Disattiva`: a schermo stretto le azioni vanno a capo dentro `.question__actions` e la domanda vera è molto più alta del suo scheletro. Pre-esistente dalla 2.4 con `Modifica`, allargato dalla 2.5 con le frecce e dalla 2.6 col quarto pulsante. La voce sugli scheletri già a ledger parla di altezze del testo di aiuto e di larghezze, non della riga delle azioni. Da riprendere insieme a quella, con la misura a 375px che la sessione di Task 5 fornirà.

## Deferred from: implementazione della story 3-1-aprire-una-scheda-di-prequalifica-su-un-cliente (9 agosto 2026)

- **La scheda cliente attende quattro letture in fila invece che in parallelo** — `src/app/(app)/clienti/[id]/page.tsx`: cliente, suggerimenti dei tag, persone e ora schede, quattro `await` uno dopo l'altro. Le ultime tre non si scambiano niente e nessuna dipende dal risultato delle altre: solo la prima deve arrivare per prima, perché `notFound()` dipende da lei. La latenza del database si somma quattro volte sulla schermata dove si sta durante una call. È la stessa voce già aperta sulla schermata iniziale dalla revisione della 1.7 («le due letture sono attese in serie»), che questa story allarga da due a quattro su un'altra rotta. Rimandato: `AGENTS.md` vieta il refactor non concordato e la story 3.1 non chiede prestazioni; un `Promise.all` sulle tre indipendenti non cambia nessun'altra riga. Da chiudere insieme all'altra, in una decisione sola.
- **La firma di `open_assessment` è stata corretta dopo l'applicazione, e la riga di registro riscritta** — `supabase/migrations/0013_open_assessment.sql`. La prima applicazione dichiarava `p_call_date date, p_interviewee_id uuid` senza `default`, e i tipi generati da Supabase rendono un argomento senza default **obbligatorio e non nullo**: passare «nessun interlocutore» avrebbe richiesto un `as`, vietato da `kb-0.md` §2. La correzione è stata fatta eseguendo l'annullamento che il file stesso dichiara (`drop function open_assessment(uuid, date, uuid);`), cancellando la riga di registro della prima applicazione e riapplicando il file corretto, così che un file corrisponda a una riga. Quello che resta scoperto è la regola generale, che nessun documento scrive: **un parametro di funzione che può essere nullo deve avere `default null`, altrimenti il tipo generato mente**. Vale per la 0011 (nessun parametro nullabile, quindi non si è mai visto) e per ogni funzione futura. Da scrivere in `database.md` §7 la prima volta che una story aggiunge un'altra funzione con parametri facoltativi, quando ci saranno due casi invece di uno. **La revisione del 9 agosto 2026 aggiunge la seconda metà scoperta**: `database.md` §7 autorizza un `update` su `supabase_migrations.schema_migrations` per il caso del prefisso mancante, non un `delete` per una riapplicazione, e quella è la manovra che è stata fatta. Verificata innocua — una sola riga a registro, le sue `statements` contengono i `default null`, la definizione a database è identica al file, e la funzione non era mai arrivata in produzione — ma resta una pratica che nessun documento copre. Si scrive insieme alla regola dei `default null`, e non prima: due righe di procedura scritte su un caso solo sono due righe che il secondo caso smentisce.

## Deferred from: code review of 3-1-aprire-una-scheda-di-prequalifica-su-un-cliente (9 agosto 2026)

- ~~**`coalesce(p_call_date, current_date)` risolve «oggi» in UTC, non nel fuso dichiarato**~~ — **chiusa il 9 agosto 2026, prima di essere rimandata davvero.** Luca ha deciso di fare la migrazione 0014 per la corsa su `total_questions`, e le due correzioni stanno nella stessa `create or replace` sulla stessa funzione: rimandare questa avrebbe voluto dire riaprire `open_assessment` una seconda volta per una riga. Il testo resta qui perché il motivo della correzione è la parte che serve fra sei mesi. `supabase/migrations/0013_open_assessment.sql:106`. `current_date` si valuta nel `TimeZone` della sessione, che su Supabase è UTC e che né `authenticator` né `authenticated` sovrascrivono. È lo stesso difetto che `todayIsoDate()`, i suoi test e la prop `today` filata fino al modulo esistono per impedire (D23): una chiamata che omette la data fra mezzanotte e le due di Roma scriverebbe **ieri**. Non è raggiungibile dall'azione di oggi, perché `validateCallDate` restituisce sempre una data e `openAssessment` la passa sempre. Rimandato: il parametro ha `default null` proprio per essere omissibile e la funzione è `grant execute … to authenticated`, quindi la trappola è viva per la Story 3.2 in poi, e il commento in testa al file la presenta come comportamento corretto. Le due strade sono `coalesce(p_call_date, (pg_catalog.now() at time zone 'Europe/Rome')::date)` — che mette il fuso in un secondo posto e va dichiarato — oppure rendere `p_call_date` obbligatorio e togliere il ripiego, così che la regola resti in un posto solo. **Si chiude con la stessa migrazione della corsa su `total_questions`**, se quella viene aperta: due correzioni allo stesso file in una `create or replace` sola. Lo stesso difetto latente sta nel `default current_date` della 0005, che è pre-esistente.
- **La prop `today` invecchia a cavallo della mezzanotte, e il modulo registra ieri** — `src/app/(app)/clienti/[id]/page.tsx:132` con `new-assessment-form.tsx:68`. `todayIsoDate()` si valuta una volta, quando la pagina si rende. Una scheda cliente aperta alle 23:58 e un `Apri scheda` alle 00:03 mandano un `call_date` **non vuoto** pari a ieri, che `validateCallDate` accetta verbatim: il ripiego su `today` scatta solo sul campo svuotato. È lo stesso esito che `todayIsoDate` è stata scritta per impedire, per un'altra strada. Rimandato: il valore è **visibile** nel campo prima di premere — `<input type="date">` mostra la data proposta — quindi non è silenzioso, la finestra richiede una scheda lasciata aperta a cavallo della mezzanotte, e ogni correzione riporta il calcolo della data nel browser, cioè esattamente la discordanza di idratazione che la story ha deciso di evitare. Da riprendere se la Story 3.2, che terrà la schermata aperta molto più a lungo, rende la finestra frequente. **La Story 3.2 non la rende frequente, il 9 agosto 2026, e vale la pena dire perché.** La schermata di compilazione **sì** si tiene aperta per ore, ma la prop `today` non ci arriva: quella schermata non porta nessun campo data e non chiama `open_assessment` — l'unico `<input type="date">` resta in `new-assessment-form.tsx`, dentro la card del cliente, che si apre e si chiude in pochi secondi. La finestra è quindi ancora «una scheda **cliente** lasciata aperta a cavallo della mezzanotte», invariata. Il rimando resta, con il punto di ripresa spostato: non più «se la 3.2 allunga la sessione», ma il giorno che un campo data compare su una schermata dove si sta a lungo.
- **Una `position` duplicata verrebbe congelata per sempre dentro `answers.position`** — `supabase/migrations/0013_open_assessment.sql:121-127`. `database.md` §8 dichiara che un duplicato nato dalla corsa nota sull'insert delle domande (accettata dalla 2.2) «si sana alla prima rinumerazione». `answers.position` non ha nessuna auto-guarigione: l'indice è normale e non unico, e niente rinumera mai una scheda — per scelta, ed è la decisione 2 di questa story. Una scheda aperta dentro quella finestra avrebbe due risposte con la stessa posizione e un ordine indefinito per sempre. Rimandato: le posizioni oggi sono `1..23` senza duplicati, la causa è la corsa pre-esistente e non questa story, e il commento del file difende bene i *buchi* — che sono un'altra cosa. Da chiudere insieme alla corsa della 2.2, non prima. **Correzione dalla Story 3.2, il 9 agosto 2026**: «un ordine indefinito per sempre» non è più vero. La lettura della schermata di compilazione ordina `.order('position').order('id')` (`clienti/[id]/schede/[assessmentId]/page.tsx`), quindi a parità di posizione l'ordine è **definito e stabile** — arbitrario, ma sempre lo stesso fra un caricamento e l'altro. Resta vero il resto della voce: due risposte con la stessa posizione sarebbero *nell'ordine sbagliato* rispetto all'intervista, e niente le rinumera. Il danno passa da «ordine che balla» a «due domande scambiate», che è meno grave e altrettanto silenzioso.
- **La macchina dei quattro stati col ritorno di fuoco è ora scritta tre volte** — `src/app/(app)/clienti/[id]/assessments-card.tsx:39-95` contro `people-card.tsx:22-51`. Identiche riga per riga con i sostantivi cambiati: i due `useState`, la ref sul titolo con `tabIndex={-1}`, l'`useEffect([returning])`, `close()`, il pulsante d'intestazione sotto `count > 0 && !opening`, lo stato vuoto sotto `!== null && count === 0 && !opening`, l'`ErrorState` con `retryHref`. Rimandato: la spec lo prescriveva esplicitamente («è la forma di `PeopleCard` riga per riga»), `useWrite` è già stato estratto per una duplicazione analoga, e la seconda copia non è ancora un costo. Da chiudere alla terza, che è l'Epic 4 — e va chiusa insieme alla voce sullo stato d'errore senza modo di creare, che riguarda le stesse tre card.
- **`globals.css` è stato aperto su un giudizio di progetto, non sul bisogno che il perimetro autorizzava** — `src/app/globals.css:773-795`, le tre regole `.assessments`, `.assessment` e il suo `:first-child`. Il Task 4 diceva «se a schermo emerge un bisogno reale (**verifica a 375px, Task 7**), si dichiara nel Dev Agent Record **invece di anticiparlo**», e le Project Structure Notes davano `globals.css` per invariato «salvo bisogno dichiarato a schermo». Lo scostamento 3 è dichiarato e la motivazione è di progetto e non di misura («il respiro di `.people` è quello di una griglia di campi»); la verifica a 375px che avrebbe stabilito il bisogno è una delle undici caselle riservate alla sessione, e quella eseguita è una riproduzione statica, come il record stesso dice. Rimandato: le tre regole sono minime, senza esadecimali e sulla scala di spaziatura, e un `<ul>` ha comunque bisogno del suo azzeramento. **Lo chiude la verifica a 375px della sessione**: se la resa regge, la voce si cancella; se no, è lì che si vede cosa serviva davvero. **Annotazione dalla Story 3.2, il 9 agosto 2026: la voce resta aperta, ma il metodo che chiedeva è ora stato applicato.** Le tre regole della 3.1 non sono state riprovate — la riproduzione statica della 3.2 rende la schermata di compilazione, non la card delle schede — quindi la verifica di sessione resta la sola che possa chiuderle. Quello che è cambiato è come `globals.css` è stato aperto stavolta: delle sei regole nuove, cinque erano dichiarate nel Task 4 e la sesta — `.page-header__lead` — è nata **dopo** aver visto a schermo che `.page-header` con tre figli manda il titolo in mezzo alla riga, cioè da una misura e non da un giudizio. È la forma che questa voce chiedeva; qui si registra perché chi la chiuderà sappia che il precedente esiste.

## Deferred from: code review of 3-2-compilare-le-risposte-durante-la-call (9 agosto 2026)

- **Un `content` degenere nella scelta singola: soli spazi genera un'opzione invisibile, una stringa vuota fra le opzioni duplica «Nessuna risposta»** — `src/app/(app)/clienti/[id]/schede/[assessmentId]/answer-field.tsx:130` e `:136-141`. Due casi con la stessa radice: il confronto guarda la stringa vuota **esatta** e non il vuoto dopo `trim`. Con `content = '   '`, `content !== ''` passa e `options.includes` fallisce, quindi nasce `<option value="   ">   </option>`: nel menu compare una riga vuota e, da selezionata, il campo si legge vuoto — indistinguibile da «Nessuna risposta», cioè esattamente la confusione che l'opzione orfana esiste per evitare. Con un elemento `''` dentro `answers.options` ci sono due `<option value="">` nello stesso selettore, React associa il valore alla prima — «Nessuna risposta» — e quella risposta non è più raggiungibile. Rimandato: **oggi nessuno dei due è raggiungibile**, perché la 3.2 non scrive niente e `open_assessment` scrive `content` a `null` su tutte le righe, mentre `parseOptions` scarta le righe vuote sul lato in cui si scrive. Da chiudere con la **Story 3.3**, che è la prima che rende `content` scrivibile: è lì che «soli spazi» diventa un valore che può davvero arrivare, ed è lì che la correzione — un confronto sul testo ripulito invece che sulla stringa esatta — costa una riga dentro codice che si sta comunque toccando.
- **`answers` copia il titolo del blocco e non la sua identità: due blocchi omonimi adiacenti diventano una card sola, per sempre** — `src/lib/answer-groups.ts:30-44` con `supabase/migrations/0015_answer_question_copy.sql`. La 0015 aggiunge `answer_type`, `options` e `help_text` e **non** `block_id`, quindi l'unica identità che una risposta ha del proprio blocco è il titolo. `groupAnswersByBlock` raggruppa per corse consecutive, che è la forma giusta contro il caso non adiacente — coperto dal test — e non contro quello adiacente: due blocchi **diversi** con lo stesso titolo, uno dopo l'altro nell'ordine di `position`, si fondono in un gruppo solo, e la scheda rende una card in meno senza nessun errore. Dalla stessa radice viene il sintomo inverso: una domanda nata dentro la finestra di rinumerazione fallita (`database.md` §8) congela una `position` fuori posto e apre in coda un **secondo** gruppo con lo stesso titolo. Niente rinumera mai una scheda, quindi in entrambi i casi la deriva è definitiva. Rimandato per decisione di Luca del 9 agosto 2026, con questo motivo: `kb-0.md` §4 vieta i campi «utili un giorno», e `block_id` non serve a rendere niente di quello che sta a schermo oggi; il danno è cosmetico — le domande restano tutte, nel loro ordine e col loro testo — e richiede che Luca scriva due volte lo stesso titolo su due blocchi vicini. **Il costo del rimando è dichiarato:** oggi la colonna si aggiungerebbe `not null` senza riempimento, perché `answers` è vuota, ed è la stessa finestra gratuita che ha reso gratuita la 0015; dalla prima scheda vera in poi servirà una colonna nullabile e un riempimento da `answers.question_id → questions.block_id`, che copre solo le domande non ancora cancellate. **Da riprendere** il giorno che due blocchi omonimi esistono davvero, o insieme alla corsa della 2.2, con cui condivide il secondo sintomo.
