# Lavoro rimandato

Voci emerse durante le revisioni e rimandate di proposito, con il motivo. Si cancellano solo quando sono chiuse.

## Deferred from: code review of 1-1-creare-un-cliente-col-solo-nome (2 agosto 2026)

- **`.btn` è alto 40px, sotto il bersaglio da 44px** — `src/app/globals.css:299-311`. Governa tutti e quattro i pulsanti nuovi di questa story (`Salva`, `Annulla`, `Nuovo cliente`, `Crea il primo cliente`). Rimandato: la classe è preesistente e fuori dal perimetro, e cambiarla tocca ogni schermata. Appartiene alla Story 5.2, passata di accessibilità verificata.
- **L'hover illumina tutta la riga ma solo il nome è cliccabile** — `src/app/(app)/clienti/page.tsx:77-79` con `src/app/globals.css:250-252`. L'affordance promette una riga cliccabile, il comportamento dà una cella. Rimandato: la scelta fra estendere il collegamento alla riga o restringere l'hover dipende da come le Story 1.2 e 1.7 trattano la navigazione di riga.
- ~~**Nel ramo d'errore dell'elenco non resta nessun modo di creare un cliente**~~ — `src/app/(app)/clienti/page.tsx:30-39, 43-47`. Il pulsante primario compare solo nel ramo pieno e in quello vuoto; se la query fallisce resta solo Riprova. Rimandato: in quello stato Riprova è l'azione giusta, e aggiungere un secondo pulsante primario violerebbe UX-DR10. **Chiusa nella revisione della Story 1.7, il 5 agosto 2026.** Il motivo del rimando era scaduto: `ErrorState` rende `Riprova` come `btn btn--secondary` (`components/error-state.tsx:11`), quindi `Nuovo cliente` nell'intestazione non è una *seconda* primaria, è l'unica, e UX-DR10 non viene toccata. La condizione dell'intestazione è ora `error || found !== null || (filtered && !archiveEmpty)`. Resta aperta la sorella sulla card delle persone (revisione della 1.5), che è un file fuori dal perimetro di questa story.
- **Nessun `metadata` sulle rotte nuove** — `src/app/(app)/clienti/[id]/page.tsx`, `src/app/(app)/clienti/nuovo/page.tsx`. Il titolo del browser resta "Registro" per ogni cliente: con due schede aperte diventa un indovinello. Rimandato: fuori dal perimetro della story, appartiene a una passata di rifinitura.

## Deferred from: creazione della story 1-2-vedere-e-modificare-la-scheda-del-cliente (2 agosto 2026)

- **L'alone del focus usa la tinta sbagliata su tutte le schermate** — `src/app/globals.css` (`.input:focus`). `.input:focus` illumina in `--sec-prequalifica`, mentre UX-DR14 chiede la tinta della sezione corrente, che nell'area clienti è `--sec-clienti`. Emerso perché la Story 1.2 riempie una schermata di clienti di campi e rende la discordanza visibile ovunque. Rimandato per decisione di Luca del 2 agosto 2026: la correzione tocca ogni campo del progetto, `/accedi` compreso, e appartiene alla Story 5.2, passata di accessibilità verificata.

## Deferred from: code review of 1-2-vedere-e-modificare-la-scheda-del-cliente (3 agosto 2026)

- **Fra l'ultima battuta e i tre secondi non c'è protezione** — `src/app/(app)/clienti/[id]/client-field-form.tsx:124`. Chiudere la scheda o ricaricare mentre la textarea ha ancora il fuoco perde quello che è stato scritto: allo smontaggio c'è `clearTimeout` e nient'altro, nessun `beforeunload` e nessuna scrittura di chiusura. Rimandato per decisione di Luca del 3 agosto 2026: la finestra è di tre secondi e solo col fuoco ancora dentro, e il salvataggio all'uscita dal campo copre già ogni navigazione dentro l'applicazione. Da riprendere se la Story 3.3 rende la finestra più frequente, o se un `beforeunload` diventa accettabile.
- **I due campi lunghi che salvano insieme possono far tornare indietro `baseline`** — `src/app/(app)/clienti/[id]/client-field-form.tsx:38` con `src/app/(app)/clienti/actions.ts:169`. `business_goals` e `notes` hanno due `useActionState` distinti, quindi React non li mette in fila. Ognuna delle due azioni chiama `revalidatePath` sulla scheda e produce un payload reso al proprio momento: se quello partito prima arriva dopo, il campo che ha già scritto riceve un `stored` precedente alla propria scrittura, `dirty` torna vero su un campo salvato e il timer rispara lo stesso testo. Rimandato: dipende dall'ordine di consegna dei payload RSC, non è stato riprodotto, e si autocorregge al giro successivo. Da riprendere se la Story 3.3, che avrà molti più campi che salvano da soli sulla stessa schermata, lo rende frequente.

## Deferred from: code review of 1-3-cambiare-lo-stato-del-cliente (3 agosto 2026)

- **Uno stato fuori dai cinque si mostra come `potenziale` senza dirlo** — `src/app/(app)/clienti/[id]/client-field-form.tsx:127`. Il `<select>` rende solo le cinque `<option>` di `CLIENT_STATUSES`; quando `value` non corrisponde a nessuna, React seleziona la prima opzione non disabilitata. La scheda dichiara `potenziale` su un valore diverso, e siccome `value === baseline` non compare nemmeno `Salva`: il valore ignoto resta nel database senza modo di uscirne. `StatusPill` gestisce apposta lo stesso caso (`status-pill.tsx:26`), il selettore no. Rimandato: la colonna è `not null` con `check` sui cinque valori, quindi oggi il caso non esiste. Da chiudere il giorno che uno stato entra nel vincolo, insieme alla mappa della pillola.
- **`save()` non controlla `dirty`: un Invio sul selettore riscrive lo stesso stato** — `src/lib/use-editable-field.ts:144`. `save: () => { if (!pending) write(value) }`. Un `<select>` non blocca l'invio implicito del form, quindi Invio a campo non modificato arriva a `onSubmit` e scrive: il trigger sposta `updated_at`, `revalidatePath('/clienti')` riordina, e il cliente salta in cima per un tasto premuto. Rimandato: vale identico per i dieci campi corti dalla Story 1.2, e quel file è appena uscito da una revisione che la story dichiara intoccabile oltre la riga dei tipi. Da chiudere in una passata sola su tutti i campi.
- **Un `baseline` che cambia non risincronizza `value`** — `src/lib/use-editable-field.ts:60`. `useState(baseline)` inizializza una volta e nulla riallinea il valore quando la prop cambia: con la stessa scheda aperta in due posti, la scrittura dell'altro fa comparire `Salva` e `Annulla` da soli, e quel `Salva` riscrive il valore vecchio sopra il nuovo. Rimandato: preesistente dalla Story 1.2, vale per ogni campo, e con un utente solo lo scenario è raro. Parente della voce già rimandata sui due campi lunghi che salvano insieme.
- **Cinque ternari annidati nella resa del campo** — `src/app/(app)/clienti/[id]/client-field-form.tsx:108`. Il file è passato da 157 a 175 righe e resta sotto il limite, ma la catena rende ormai cinque controlli diversi con i commenti dentro i rami, e il sesto `kind` — i tag della Story 1.4 — finisce lì dentro. Rimandato: oggi il limite di `kb-0.md` §2 è rispettato e la story vieta esplicitamente un secondo componente. Da riprendere quando la Story 1.4 aggiunge il sesto ramo: lì la scelta è fra una mappa `kind → renderer` e la divisione del file. **La Story 1.4 non ha aggiunto il sesto ramo**: i tag hanno un componente proprio e questo file non è stato aperto. La voce resta com'era.

## Deferred from: code review of 1-4-marcare-i-clienti-con-tag-riusabili (3 agosto 2026)

- **`maxLength` tronca l'incolla in silenzio e rende irraggiungibile il messaggio del limite** — `src/app/(app)/clienti/[id]/client-tags-form.tsx:166`, `src/app/(app)/clienti/[id]/client-field-form.tsx:120`, `src/app/(app)/clienti/nuovo/new-client-form.tsx:50`. L'attributo `maxLength` porta lo stesso numero che il server usa come soglia di rifiuto: dall'interfaccia il limite non si supera mai, quindi il messaggio che spiega cosa fare è codice che nessuno vedrà, e quello che l'utente vede è invece un incolla tagliato senza avviso. Rimandato: il pattern esiste in tre punti con due costanti diverse (`CLIENT_NAME_MAX_LENGTH` e `CLIENT_TAG_MAX_LENGTH`), è arrivato con la Story 1.1 e si chiude in una passata sola. La strada è togliere `maxLength` e lasciare parlare il server, oppure dire a schermo quanto manca.
- **Nessuna normalizzazione Unicode, e `INVISIBLE` copre meno di quanto il commento promette** — `src/lib/client-tags.ts:29` con `src/lib/validate-client-name.ts:15`. `normalizeTag` toglie invisibili, comprime spazi e mette in minuscolo, ma non normalizza le forme Unicode: `città` in forma decomposta (`a` + U+0300, quello che arriva da molti incolla) e `città` battuto a tastiera restano due tag distinti che si leggono uguali, e il controllo dei doppioni non li vede. `INVISIBLE` è `[U+200B–U+200D, U+2060, U+FEFF]`: fuori restano U+00AD, U+180E e i controlli di direzione U+202A–U+202E, tutti invisibili e tutti frequenti nel testo incollato dal web. Rimandato: è lo stesso buco di `normalizeClientName` dalla Story 1.1, non introdotto qui, e si chiude aggiungendo `.normalize('NFC')` in un posto solo. Da riprendere insieme, perché la costante è condivisa.
- **La query dei suggerimenti non ha `.limit()` e PostgREST tronca in silenzio** — `src/app/(app)/clienti/[id]/page.tsx:75`. `supabase.from('clients').select('tags')` senza limite né ordinamento: oltre il tetto di righe di PostgREST i suggerimenti diventano incompleti e niente lo segnala, e il commento accanto giustifica il costo della lettura, non il troncamento. Rilegge anche la riga del cliente corrente, che è già in `data`. Rimandato: è lo stesso pattern di `findExistingName` (`actions.ts:353-356`), oggi i clienti sono qualche decina, e la strada vera è la migrazione con `unnest` che l'Epic 1 dichiara di non portare. Da riprendere se i clienti superano il migliaio, o insieme alla Story 1.7 che tocca comunque i tag. **La Story 1.7 non la chiude, e la allarga** (4 agosto 2026): lo stesso schema esiste ora anche in `src/app/(app)/clienti/page.tsx`, cioè sulla schermata iniziale, per costruire il selettore dei tag. Al tetto di righe di PostgREST quel selettore offre un elenco incompleto, quindi un tag che esiste diventa non filtrabile e niente lo dice — un difetto che non produce un errore, produce un filtro che non si può scegliere. La strada resta la migrazione con `unnest`.
- **Nessun aggiornamento di stato è dentro `startTransition`** — `src/app/(app)/clienti/[id]/client-tags-form.tsx:81` con `src/lib/use-editable-field.ts:88`. `startTransition(() => { void action(…).then(…) })`: la funzione di scope è sincrona, quindi solo l'invocazione dell'azione è marcata come transizione, mentre `setPending`, `setError` e `setDraft` girano in un microtask successivo, fuori dal tracciamento, come aggiornamenti urgenti. Il campo si svuota nel commit precedente a quello che porta il chip rivalidato, e su rete lenta c'è una finestra senza né il testo digitato né la conferma. Rimandato: il pattern arriva da `use-editable-field.ts` ed è stato deciso in revisione il 3 agosto 2026 per un motivo che resta valido (non passare da `useActionState`); la forma corretta è una funzione di scope `async` con `await`, e va cambiata nei due file insieme.
- **La corsa lettura-scrittura sull'array `tags`** — `src/app/(app)/clienti/actions.ts:353`+`376` e `:423`+`441`. `addClientTag` e `removeClientTag` leggono `tags`, lo ricompongono e riscrivono l'array intero: con la stessa scheda aperta in due posti, due scritture in volo insieme leggono lo stesso elenco di partenza e la seconda sovrascrive la prima. Il tag aggiunto dall'altra parte sparisce senza errore. Rimandato per decisione di Luca del 3 agosto 2026: l'alternativa è una funzione nel database che faccia `array_append` in una sola istruzione, cioè una migrazione nuova per una corsa che richiede due schede aperte sullo stesso cliente nello stesso secondo, con un utente solo (D9), mentre `AGENTS.md` dichiara che l'Epic 1 non porta migrazioni. La motivazione completa sta in `1-4-marcare-i-clienti-con-tag-riusabili.md`, sezione «Una corsa nota, e perché si accetta»; qui resta la voce perché fuori da quel file non era visibile a nessuno. Da riprendere il giorno che gli utenti diventano più di uno, insieme alle policy, che quel giorno cambiano comunque.

## Deferred from: code review of 1-5-registrare-le-persone-e-chi-decide (4 agosto 2026)

- **`PERSON_GONE` esce senza rivalidare: la riga fantasma resta a schermo e interattiva** — `src/app/(app)/clienti/people-actions.ts:151`, `:198`, `:332`. Quando la persona non c'è più, i tre rami `!data` di `updatePersonField`, `updatePersonRoles` e `deletePerson` ritornano prima di qualsiasi `revalidatePath`: il blocco resta montato con i suoi sei campi, le sue caselle e i suoi pulsanti, tutti destinati a rispondere di nuovo lo stesso messaggio. `setPersonPrimary` invece rivalida (`:280`), perché lì il `client_id` è già in mano. Rimandato: senza la riga non c'è `client_id`, e prenderlo dal `formData` è esattamente quello che la story vieta per non lasciar scegliere al browser quale scheda toccare. Il messaggio dice già «Ricarica la pagina», quindi il ripristino esiste. Da riprendere se si decide che `revalidatePath` non è un confine di sicurezza e può accettare un percorso suggerito dal client.
- **Lo stato locale dei ruoli non si risincronizza quando il dato cambia da fuori** — `src/app/(app)/clienti/[id]/person-roles-form.tsx:27`. `useState(stored)` inizializza una volta sola e solo il callback di successo riallinea: con la stessa scheda aperta in due posti, la scrittura dell'altra fa comparire `Salva` e `Annulla` da soli su caselle ferme al valore vecchio, e quel `Salva` riscrive il valore superato sopra il nuovo. Rimandato: è la stessa voce già aperta su `use-editable-field.ts:60` dalla Story 1.3, vale identica per i sei campi di testo della persona, e si chiude in una passata sola su tutti i campi invece che componente per componente.
- **Nessun limite di attesa in `useWrite`: i controlli restano spenti finché la rete non decide** — `src/lib/use-write.ts:56`. `await call().then(it => it, () => null)` intercetta il rifiuto ma non il non-arrivo: se la richiesta non si risolve né si rifiuta — passaggio wifi/cellulare su tablet, dispositivo che si sospende — il controllo resta `disabled` e muto per tutto il timeout del browser, senza errore. Sulla spunta del contatto principale, che per scelta non ha indicatore di salvataggio, il risultato è indistinguibile da un tocco non registrato. Rimandato: `use-editable-field.ts:91` ha esattamente la stessa forma dalla Story 1.2, e un `AbortSignal.timeout` va messo nei due file insieme o in nessuno.
- **Nello stato d'errore della card delle persone non resta nessun modo di aggiungerne una** — `src/app/(app)/clienti/[id]/people-card.tsx:52-57`. Con `people === null` il conteggio vale 0, quindi né il pulsante dell'intestazione né quello dello stato vuoto si rendono: la card è un riquadro d'errore e basta, e `Riprova` è un `<Link>` verso la rotta su cui si è già. Rimandato: è la stessa forma della voce già aperta sull'elenco clienti («nel ramo d'errore non resta nessun modo di creare un cliente»), il comportamento di `ErrorState` è quello di tutto il progetto, e in quello stato `Riprova` è l'azione giusta. Da chiudere insieme all'altra, quando si decide cosa fa davvero `Riprova` su una rotta già aperta.
- **Nessun limite di lunghezza sui sei campi della persona** — `src/app/(app)/clienti/people-actions.ts:77` e `:132`. `normalizeTextValue` fa solo `trim()` e «stringa vuota → `null`»: nessun `maxLength` sugli input, nessun tetto nell'azione, sei colonne `text` che accettano una richiesta di dimensione arbitraria. Task 1 della story lo dichiara voluto in nome di D14, e la domanda 6 lo conferma, ma D14 parla di non vietare azioni, non di non porre limiti tecnici: `name` ha 200 caratteri e i tag 40, e `kb-0.md` §3 chiede che ogni input si validi sul server con uno schema. Rimandato: è la stessa passata della voce sul `maxLength` che tronca in silenzio, e le due si chiudono insieme decidendo una volta sola dove sta il limite e chi lo dice a schermo.

## Deferred from: code review of 1-6-ordinare-l-elenco-per-ultima-attivita-vera (4 agosto 2026)

- **Innesto e clienti senza `.limit()`: PostgREST tronca in silenzio** — `src/app/(app)/clienti/page.tsx:33`. La `select` non limita né i clienti né le schede innestate. Al tetto di righe di PostgREST la finestra viene scelta ordinando per `clients.updated_at`, mentre la chiave d'ordine vera è l'ultima attività: un cliente recente solo per una scheda viene tagliato prima di essere ordinato, e l'elenco esce in un ordine sbagliato ma plausibile. È la stessa categoria della voce sui suggerimenti dei tag, e la stessa che AC4 dichiarava di voler proteggere. L'ottimizzazione che l'avrebbe contenuta — `.limit(1, { referencedTable: 'assessments' })` — è stata scartata perché non verificabile su una tabella vuota. Da riprendere quando le schede esistono, o al migliaio di clienti, qualunque arrivi prima.
- ~~**L'elenco clienti dipende da una tabella mai interrogata in produzione**~~ — `src/app/(app)/clienti/page.tsx:33`. **Chiusa il 4 agosto 2026**, con la verifica di sessione di Task 6 della story 1.6: la query con l'innesto è stata eseguita con una sessione vera e ha risposto, e nessun cliente è sparito dall'elenco. Resta vero che l'innesto sta nella stessa richiesta, quindi un problema futuro su `assessments` farebbe cadere l'intera schermata iniziale sullo stato d'errore invece di darla con una colonna in meno: da riguardare quando le schede esisteranno davvero.
- **`npm test` non è attraversato da nessun cancello** — niente `.github/`, niente `.husky/`, e `next build` non esegue i test. Il comando esiste (AC4 chiedeva quello) ma i due guardiani appena scritti proteggono solo chi si ricorda di invocarli a mano. `kb-0.md` §10 vuole che si rilasci da un branch passato dall'anteprima: oggi un test rosso non ferma nessun rilascio.
- **`allowImportingTsExtensions` allenta tutto il progetto per due file di test** — `tsconfig.json:12`. Rende legale `import … from './foo.ts'` ovunque sotto `**/*.ts`, codice di rotta compreso, e non c'è nessun linter nel progetto che lo impedisca. Una regola di compilazione globale cambiata per una comodità locale. **La Story 1.7 la allarga** (5 agosto 2026): `src/lib/client-tags.ts` e `src/lib/client-filters.ts` usano ora `.ts` sui propri import e stanno **nel grafo dell'app**, non solo in quello dei test, quindi non si tratta più di «due file di test». La regola vera — un modulo di `src/lib` raggiunto da un test deve usare `.ts` su tutta la propria catena, altrimenti il test muore con `ERR_MODULE_NOT_FOUND` da un file che chi lo scrive non ha toccato — non è scritta in nessun file di istruzioni, e né `tsc --noEmit` né `next build` possono segnalarla. In revisione è stata messa come commento su `client-tags.ts:1`, che è il posto dove la trova chi inciampa; il posto giusto se la regola si conferma è `AGENTS.md`, e quel file è di Luca.
- **`lastActivityAt` ricalcolato due volte per confronto, più una per riga in resa** — `src/lib/last-activity.ts:47`. Il comparatore riscorre le schede e rifà `Date.parse` a ogni confronto. Decorare una volta e ordinare sui numeri era la forma ovvia. Oggi trascurabile, perché nessuna scheda esiste; conta perché la story 1.6 motiva l'intera scelta architetturale col costo dell'ordinamento in memoria.
- **La revalidazione di `/clienti` quando cambia una scheda** — da oggi l'ordine dell'elenco dipende anche da `assessments.updated_at`, ma nessuna azione rivalida `/clienti`: `revalidatePath('/clienti')` sta solo nelle azioni del cliente. Il giorno in cui la Story 3.3 salva una risposta, quella scrittura deve rivalidare anche l'elenco, altrimenti si lavora un'ora su una scheda, si torna all'elenco e il cliente è dov'era.

## Deferred from: code review of 1-7-cercare-e-filtrare-l-elenco (5 agosto 2026)

- **Le due letture della schermata iniziale sono attese in serie invece che in parallelo** — `src/app/(app)/clienti/page.tsx:68` e `:75`. `await query.order(…)` e `await supabase.from('clients').select('tags')` stanno una dopo l'altra, ma non si scambiano niente: nessun parametro della seconda dipende dal risultato della prima. La latenza del database si somma invece di sovrapporsi, e succede sulla schermata che si apre più spesso. Un `Promise.all` non cambia nessun'altra riga. Rimandato: `AGENTS.md` vieta il refactor non concordato, e la story non chiede prestazioni. Da chiudere quando si tocca comunque quel blocco, o se la schermata iniziale diventa lenta.

## Deferred from: code review of 1-8-poter-tornare-indietro-da-una-modifica-di-schema (5 agosto 2026)

- ~~**`database.md` §8 contraddice il commento nuovo di `0004`**~~ — `docs/database.md` §8 diceva che «il seed crea il questionario vuoto e le domande si inseriscono dall'interfaccia», mentre la dichiarazione aggiunta a `supabase/migrations/0004_questionnaire.sql` dice che il seed rimette le 23 domande approvate. Contate: `supabase/seed.sql` ne inserisce davvero 23 in 8 blocchi, quindi il commento nuovo ha ragione e §8 era rimasta indietro. Rimandato: il perimetro della Story 1.8 limitava `database.md` alla §7, e riscrivere §8 senza averla in perimetro sarebbe stata deriva del piano. **Chiusa dalla Story 2.1, il 5 agosto 2026**, che è la prima story dell'Epic 2 e legge quelle tre tabelle per la prima volta. §8 ora dice quello che il seed fa davvero — 8 blocchi e 23 domande, rieseguibile, da eseguire dopo il primo accesso — e la parte sul «finché le domande non sono state riviste» è caduta perché la revisione è la decisione D19. Nello stesso passaggio §8 ha preso anche la nota sulle posizioni globali, che era la scoperta della Story 2.1 e non aveva un posto dove si cerca.
- **Nessuna versione di Node dichiarata nel progetto** — `package.json` non ha `engines`, e non esiste nessun `.nvmrc`. `supabase/migrations.test.ts` usa `import.meta.dirname` (Node ≥ 20.11) e l'intero comando `test` esegue `.ts` direttamente da `node --test` (≥ 22.6): due soglie reali che nessun file dichiara. La versione verificata, `v24.13.0`, è scritta solo dentro i file delle story, che nessuno strumento legge. Su una macchina o una CI con Node più vecchio l'errore è criptico e sembra un guasto del test invece che una versione sbagliata. Rimandato: pre-esistente dalla Story 1.6, che ha introdotto l'esecuzione dei test in TypeScript; la Story 1.8 ha solo alzato di poco la soglia. Da chiudere insieme alla voce sul cancello che manca a `npm test`, perché è la stessa passata: chi mette il cancello deve comunque dire su quale Node gira.
- **`readdirSync` non è ricorsivo e filtra solo `.sql` minuscolo** — `supabase/migrations.test.ts:39-41`. Una migrazione dentro una sottocartella di `supabase/migrations/`, o chiamata `0008_x.SQL`, non viene mai letta: il controllo non la vede e non produce nessun rosso, cioè esattamente il difetto che esiste per intercettare. Rimandato: oggi la cartella è piatta e la Story 1.8 vieta esplicitamente strutture nuove, rinomine e rinumerazioni, quindi non c'è nessuna istanza. Da riprendere il giorno che la cartella smette di essere piatta — se le migrazioni diventano tante e si archiviano per anno, quel giorno il controllo va reso ricorsivo nella stessa modifica, non dopo.

## Deferred from: code review of 2-1-vedere-il-questionario-com-e-oggi (6 agosto 2026)

- **Il messaggio d'errore diagnostica, e diagnostica male il guasto più probabile di quella query** — `src/app/(app)/questionario/page.tsx:86`. «La connessione al database non ha risposto» dichiara una causa che il codice non conosce: la cosa nuova in quella lettura è la sintassi `referencedTable: 'question_blocks.questions'`, e se smettesse di essere accettata la risposta sarebbe un `PGRST100` o un `42703` — il database avrebbe risposto benissimo, e l'utente leggerebbe il contrario. `kb-0.md` §3 chiede un messaggio che dica *cosa fare*. Rimandato: è replicato alla lettera da `clienti/page.tsx:149`, quindi è pre-esistente e vale su due schermate; si chiude in una passata sola, insieme alla voce già aperta su cosa faccia davvero un `Riprova` verso la rotta su cui si è già.
- **AC3 regge nello stato pieno e non in quello d'errore** — `src/app/globals.css:83-87`. Il ramo d'errore monta `ErrorState`, il cui `Riprova` è focalizzabile *dentro il contenuto*, e `:focus-visible` lo illumina in `--sec-clienti-tint`: un colore di sezione, per giunta di un'altra sezione, dentro il contenuto della schermata del questionario. Nello stato pieno la pagina non ha niente di focalizzabile, quindi la verifica «dentro la pagina non c'è niente di viola» non tocca mai il caso in cui la regola si rompe. Rimandato: è la voce `:focus-visible` già aperta dal 2 agosto e già assegnata alla Story 5.2, vale per ogni campo del progetto, e la Story 2.1 la dichiara esplicitamente fuori perimetro.
- **Un questionario con `is_active = false` si mostra come «non ancora caricato», con un'istruzione che non fa niente** — `src/app/(app)/questionario/page.tsx:55` e `:96-99`. La pagina filtra su `is_active = true`, zero righe portano al ramo vuoto, e la frase dice di eseguire `supabase/seed.sql`; il seed si ferma su `exists (select 1 from questionnaires where owner_id = v_owner)` (`seed.sql:26`), che **non** guarda `is_active`, quindi risponde «Questionario già presente» e torna. Il questionario con le sue 23 domande esiste, è invisibile, e la schermata prescrive un'operazione nulla: è un terzo stato collassato nel vuoto, la stessa forma di bugia che il codice evita con cura fra errore e vuoto. Rimandato: raggiungibile solo da SQL, perché nessun codice scrive `questionnaires`. Da riprendere appena l'Epic 2 tocca `questionnaires.is_active`.
- **`questions.text` e `question_blocks.title` non hanno il `check (length(trim(…)) > 0)` che `clients.name` ha** — `supabase/migrations/0004_questionnaire.sql:31,39`. Sono `not null` ma accettano la stringa vuota: una domanda diventerebbe una riga anonima con solo il badge sotto, un blocco una card con l'intestazione vuota, e niente a schermo direbbe che il campo è vuoto. Rimandato: è una differenza di schema pre-esistente e la Story 2.1 non tocca lo schema. Da chiudere nella story che introduce la scrittura — 2.2 per il titolo, 2.3 e 2.4 per il testo — perché è lì che la validazione lato server va scritta comunque, e `kb-0.md` §5 vuole il vincolo nel database e non solo nel codice.
- **Gli scheletri combaciano su una sola forma di card e a una sola larghezza** — `src/app/(app)/questionario/loading.tsx:47-49`. Il «salto 0» è stato misurato a 1280 contro il blocco 1, i cui tre testi di aiuto stanno tutti su una riga; le altre due card dello scheletro stanno al posto di blocchi il cui aiuto arriva a 159 caratteri, e ogni riga in più vale 16px che il rettangolo ad altezza fissa non ha. A 375px lo spazio di testo dentro la card scende a circa 295px e il salto torna, massimo sul dispositivo peggiore. Rimandato: la misura vera è dentro Task 7 della story, che richiede una sessione e resta a Luca; questa voce esiste perché la conclusione «salto 0» non sia letta come valida a ogni larghezza.
- **La fascia di navigazione su schermo stretto cresce con le voci** — `src/app/globals.css:125-129` e `:917-930`. `.nav` è `flex-direction: column` e l'unica regola responsive porta `.shell` a una colonna: le voci si impilano, e con due voci sono 44px + 4 di gap prima di qualsiasi contenuto. `design-system.md` §2 elenca già cinque sezioni, quindi a regime sono oltre 220px di navigazione sopra la prima domanda. Rimandato: è la Story 5.1, navigazione laterale richiudibile, ed è il suo problema per definizione. Qui si registra solo che la fascia in alto ha smesso di essere invariata: prima conteneva una voce sola.
